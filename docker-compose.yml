services:
  server:
    build:
      context: .
      dockerfile: apps/server/Dockerfile
    volumes:
      - ./apps/server:/app/apps/server
    ports:
      - "127.0.0.1:3000:3000"

  client:
    build:
      context: ./apps/client
      dockerfile: Dockerfile
    environment:
      BACKEND_PROXY_TARGET: http://server:3000
    volumes:
      - ./apps/client:/app
      - client_node_modules:/app/node_modules
    ports:
      - "127.0.0.1:5173:5173"
    depends_on:
      - server

volumes:
  client_node_modules:
